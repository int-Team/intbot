<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css" integrity="sha384-B0vP5xmATw1+K9KRQjQERJvTumQW0nPEzvF6L/Z6nronJ3oUOFUFpCjEUQouq2+l" crossorigin="anonymous">
    <title><%= username %> 님의 프로필</title>
</head>
<body>
    <div class="row py-5 px-4">
        <div class="col-md-5 mx-auto">
            <div class="bg-light shadow rounded overflow-hidden text-dark">
                <div class="px-4 pt-0 pb-4 cover">
                    <div class="media align-items-end profile-head">
                        <div class="profile mr-3">
                            <img src="<%= profile_img %>" alt="..." width="130" class="rounded mb-2 img-thumbnail">
                        </div>
                        <div class="media-body mb-5 text-black">
                            <h4 class="mt-0 mb-0"><%= username %></h4>
                            <p class="small mb-4">
                                <%= tag %>
                            </p>
                        </div>
                    </div>
                </div>
                <div class="bg-light p-4 d-flex justify-content-end text-center border">
                    <ul class="list-inline mb-0">
                        <li class="list-inline-item">
                            <h5 class="font-weight-bold mb-0 d-block"><%= money %></h5>
                            <small class="text-muted">돈</small>
                        </li>
                        <li class="list-inline-item">
                            <h5 class="font-weight-bold mb-0 d-block"><%= level %></h5>
                            <small class="text-muted">레벨</small>
                        </li>
                        <li class="list-inline-item">
                            <h5 class="font-weight-bold mb-0 d-block"><%= xp %></h5>
                            <small class="text-muted">경험치</small>
                        </li>
                    </ul>
                </div>

                <div class="px-4 py-3">
                    <h5 class="mb-0">랭킹</h5><br>
                    <div class="p-4 rounded shadow-sm bg-light border">
                        <p class="mb-0">돈 - <%= money_rank %> 위</p>
                        <p class="mb-0">레벨 - <%= level_rank %> 위</p>
                        <p class="mb-0">경험치 - <%= xp_rank %> 위</p>
                    </div>
                </div>
                <div class="px-4 py-3">
                    <h5 class="mb-0">산 물건</h5><br>
                    <div class="p-4 rounded shadow-sm bg-light border">
                        <% if (goods.length != 0) { %>
                            <% goods.forEach(e => { %>
                                <p class="mb-0"> <%= e %> </p>
                            <% }) %>
                        <% } else { %>
                            <p class="mb-0">없음</p>
                        <% } %>
                    </div>
                </div>
                <footer class="px-4 py-1">
                    <p class="mb-0">Profile by sujang958</p>
                    <br>
                </footer>
            </div>
        </div>
    </div>
</body>
</html>